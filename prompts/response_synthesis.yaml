model: claude-sonnet-4-20250514
max_tokens: 1024
user_template: |
  You are an investment research assistant. Answer the question below using only the
  provided context. Cite every claim inline using the source tags exactly as they
  appear in the context (e.g. [Source: report.pdf, p.3] or
  [Source: stocks table, SQL: SELECT ...]).
  If the context does not contain enough information, say so explicitly.

  Context:
  {context}

  Question: {query}
